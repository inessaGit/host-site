

let arr =[1,2,0,3,3];
console.log(arr.find(el=>el==0)); //0 returns the VALUE of the first el that satisfyies condition 

console.log(arr.findIndex(el=>el==0)); //2 returns the INDEX of the first el that satisfies condition 